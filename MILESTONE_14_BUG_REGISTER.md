# Milestone 14: Bug Register

Severity: **P0** critical (security/data-corruption/tenant-isolation/production-breaking) · **P1**
blocking (core enterprise workflow broken) · **P2** significant (major issue with a workaround) ·
**P3** minor (cosmetic/edge-case/low-impact). No defect was hidden for being inconvenient; the two P1
RBAC findings (BUG-019, BUG-020) were found by this milestone's own personal audit, not surfaced by
either subagent.

| ID | Page/Workflow | Description | Root cause | Severity | Affected users | Fix | Regression test | Status |
|---|---|---|---|---|---|---|---|---|
| BUG-001 | Agent Detail | Page stuck on "Loading..." forever, no error, no retry, on any fetch failure | `agentsApi.getDetail(...).then(setDetail)` had no `.catch()` | P1 | Anyone viewing an agent whose detail fetch 401/403/404/500s | Added `.catch()` + error state with Retry button | Manual trace + build; no automated test (would require mocking `agentsApi`) | **FIXED**, verified by build |
| BUG-002 | Agent Detail (Rotate certificate) | A new private key could be saved to this browser's localStorage even when the server-side rotation failed, permanently desyncing local key from the agent's real active certificate | `runAction` swallowed its own errors; `saveAgentKeyPair` ran unconditionally after `await runAction(...)` regardless of outcome | P1 | Anyone rotating a certificate during a transient backend failure | `runAction` now returns a success boolean; `saveAgentKeyPair` only runs `if (succeeded)` | Manual trace + build | **FIXED**, verified by build |
| BUG-003 | Agent Detail (Verify audit event) | Failed verification silently did nothing -- no error, no result | No try/catch around `agentsApi.verifyAuditEvent(...)` | P2 | Anyone verifying an audit event during a backend failure | Wrapped in try/catch, sets the existing `message` error state | Manual trace + build | **FIXED** |
| BUG-004 | Agent Detail (all lifecycle buttons) | No busy/disabled state -- double-click could fire duplicate mutations, no "working" feedback | `runAction` set no busy flag | P2 | Anyone double-clicking Activate/Suspend/Retire/Revoke/Rotate/Transfer | Added `pendingAction` state, all buttons disabled while any action is in flight, labels change to "-ing..." | Manual trace + build | **FIXED** |
| BUG-005 | Agent Detail (Transfer ownership) | Owner/business-unit inputs not cleared after a successful transfer | Missing state reset on success | P3 | Anyone transferring ownership | Inputs cleared only `if (succeeded)` | Manual trace + build | **FIXED** |
| BUG-006 | Agent Detail | No `useResourceSync` at all -- certificate rotation/revocation or lifecycle changes made elsewhere never reach this page while it stays mounted | Disclosed Phase 6A gap, never closed | P2 | Anyone with this page open in a second tab while an agent's certificate/status changes elsewhere | Wired `useResourceSync(["agents","certificates"], load)` | Manual trace + build | **FIXED** |
| BUG-007 | Agent Detail (lifecycle buttons) | Activate/Suspend/Retire/Revoke/Rotate/Transfer rendered with no permission check -- REVIEWER/AUDITOR/EXECUTIVE roles see clickable controls the backend 403s | Missing `hasPermission` gate, unlike the established pattern in ReviewQueuePage/VersionsPage/PolicyWorkspacePage/CorpusReviewPage | P1 (auth/RBAC) | Any non-admin role viewing an agent | Added `lacksPermission(...)` gate per action, matching backend's exact `agent.activate`/`agent.suspend`/`agent.retire`/`agent.revoke`/`agent.rotate`/`agent.manage` permissions | Manual trace + build; not yet tested under a real non-Owner session (no browser tooling) | **FIXED**, live role verification **PLANNED** |
| BUG-008 | Decision Center (Approve/Deny) | Resolve buttons rendered with no permission check -- AGENT_ADMIN/AUDITOR/EXECUTIVE roles see clickable controls the backend 403s | Missing `hasPermission("decisions.resolve")` gate | P1 (auth/RBAC) | Any role lacking DECISIONS_RESOLVE viewing a HUMAN_REVIEW decision | Added the gate plus an explanatory message ("Your role can view this decision but not resolve it.") | Manual trace + build; not yet tested under a real non-Owner session | **FIXED**, live role verification **PLANNED** |
| BUG-009 | resourceSync.ts (`useResourceSync`) | Any real cross-tab or tab-refocus signal arriving within 3 seconds of a page mounting was silently swallowed | Debounce ref initialized to `Date.now()` at mount instead of a value that never falsely suppresses the first real trigger | P2 (cross-page state integrity) | Anyone whose mutation in another tab happens to land within 3s of the dependent page's own mount | Initialize the ref to `0` instead | **Caught by, and now covered by, this milestone's new `resourceSync.test.ts`** (8 tests, all passing) | **FIXED**, regression-tested |
| BUG-010 | Policy Publish page | Page stuck on "Loading..." forever on any fetch failure, identical shape to BUG-001 | `policyStudioApi.get(...).then(...)` had no `.catch()` | P1 | Anyone viewing a policy whose fetch fails | Added `.catch()` + error state with Retry | Manual trace + build | **FIXED** |
| BUG-011 | Policy Workspace page | On fetch failure, page silently rendered as a blank "New Rule" form for what was actually an existing policy, with no error shown | `policyStudioApi.get(...).then(...)` had no `.catch()`; `isNew` is derived only from the URL param, not fetch success | P2 | Anyone editing an existing policy during a backend failure | Added `.catch()`; when `!isNew && !existing && loadError`, renders a blocking error card with Retry instead of the form | Manual trace + build | **FIXED** |
| BUG-012 | AI Authority Builder, Corpus Review | 14 parallel fetches on page load with no `.catch()` at all -- any single failing endpoint left that field permanently in its loading-skeleton state, no error, no retry | `loadAll()`'s 14 calls had no error handling | P2 | Anyone reviewing a corpus during a partial backend failure | Wrapped every fetch in a shared `track()` helper that appends to a visible `sectionLoadErrors` banner with a single "Retry all" action | Manual trace + build | **FIXED** |
| BUG-013 | AI Authority Builder, Corpus Review (answer a question) | Failed answer-save was an unhandled rejection -- clicking Save appeared to do nothing | No try/catch around `answerQuestion` | P2 | Anyone answering a clarification question during a backend failure | Added try/catch, per-question error message, and a busy state on the Save button | Manual trace + build | **FIXED** |
| BUG-014 | AI Policy Builder → promote candidate | Promoting a candidate creates a real new draft policy, but `PolicyListPage`/`RuntimePolicyDashboardPage` (both subscribed to the `"policies"` resource kind) had no way to learn it happened if left open in another tab | `aiPolicyBuilderApi.promoteCandidate` never called `notifyResourceChanged` | P2 | Anyone with a policy list page open in another tab while a candidate is promoted | Added `notifyResourceChanged("policies")` after a successful promote | Manual trace + build | **FIXED** |
| BUG-015 | Platform Organizations page | Deactivate/Reactivate/Archive fired on a single click with no confirmation -- the highest-blast-radius action in the app (an entire tenant organization), inconsistent with every other destructive action in the codebase | `transition()` wired directly to `onClick` | P1 | Any platform admin using this page | Replaced plain buttons with the existing `ConfirmButton` component (already used elsewhere for exactly this) | Manual trace + build | **FIXED** |
| BUG-016 | Organization Settings → Enterprise Systems | A newly-registered enterprise system wasn't picked up by Policy Workspace's Enterprise System dropdown without a full page remount | `createEnterpriseSystem` was the one mutation in `organization/api.ts` not emitting `notifyResourceChanged`, inconsistent with every sibling mutation in the same file | P2 | Anyone registering a system while a policy-authoring tab is open | Added the emit; wired `PolicyWorkspacePage`'s dropdown to `useResourceSync(["organization"], ...)` -- the first real consumer of the `"organization"` resource kind anywhere in the app | Manual trace + build | **FIXED** |
| BUG-017 | Live Assurance / Platform Overview | Neither page had any `useResourceSync` wiring -- rollup counts (agents, active policy, evidence/decision totals) don't refresh from mutations made elsewhere while either page stays mounted | Disclosed Phase 6A gap, never closed | P2/P3 | Anyone with either page open in a second tab during a mutation elsewhere | Wired `useResourceSync(["agents","policies","evidence"], load)` on LiveAssurance and `useResourceSync(["agents","policies"], load)` on PlatformOverview | Manual trace + build | **FIXED** |
| BUG-018 | Platform Overview | `{agentCount ?? "N/A"}` rendered "N/A" during the initial fetch, visually identical to a real unavailable state | Loading state used the same fallback text as the real error branch | P3 | Anyone loading the Overview page (momentary, self-corrects) | Changed the loading fallback to "..." | Manual trace + build | **FIXED** |
| BUG-019 | Organization Users page (revoke invitation) | Single click, no confirmation, inconsistent with the page's own "Disable user" flow two rows below it | Missing confirm gate | P3 | Anyone managing invitations | Not changed this milestone | **PLANNED** |
| BUG-020 | Organization Settings → API Keys | `create()` has no busy/creating state, unlike the adjacent Enterprise Systems tab's equivalent action -- double-click risk of creating duplicate keys | Missing busy flag | P3 | Anyone creating an API key | Not changed this milestone | **PLANNED** |
| BUG-021 | `organization/api.ts` | `createApiKey`/`revokeApiKey`/all of `usersApi` still don't emit any resource-change signal, inconsistent with the rest of the same file | No `ResourceKind` cleanly covers API keys/users today; expanding the taxonomy wasn't the smallest correct fix per this milestone's own instruction | P2 | Anyone managing API keys/users with a dependent view open elsewhere | Not changed this milestone | **PLANNED**, needs a `ResourceKind` taxonomy decision first |
| BUG-022 | `src/app/live/types.ts` | `LiveDocument`/`LiveAuthority` exported types are dead code, zero references anywhere, leftover from the already-retired legacy Authority/Mandate pipeline | Never cleaned up after that pipeline's retirement | P3 | None (dead code, no runtime effect) | Not removed this milestone -- tied to the Workstream 10 decision | **PLANNED** |
| BUG-023 | `routes.tsx` | `/governance/upload` has no forward link from any current UI surface, reachable only via legacy redirects/back-link/direct URL | Kept for backward compatibility, never given a real entry point or converted to redirect-only | P3 | A first-time user landing on this URL with no context | Not changed this milestone | **RECOMMENDED**, needs a product decision |
| BUG-024 | Backend schemas (systemic) | `status`/`health`/`outcome`/`decision`/`risk_impact` are typed as plain `str` on the backend while the frontend narrows to literal unions -- currently safe (every real value traced and matches), but a future new value would compile silently and fail only at runtime | Backend schemas never adopted `Enum`/`Literal` for these fields | P3 (latent, not live) | Anyone if a new status value is ever introduced without a matching frontend update | Not changed this milestone (backend contract change, out of scope without a separate proposal) | **RECOMMENDED** |

**Totals**: 24 items. P1: 6 (all fixed). P2: 12 (10 fixed, 2 planned). P3: 6 (2 fixed, 4 planned/
recommended). Zero P0s found. All fixes verified by a clean `npm run build` and, for BUG-009, by a
passing automated regression test; none were verified in a real browser (unavailable this session,
disclosed in the audit doc) or under a real non-Owner authenticated session (BUG-007/BUG-008's live
role behavior is explicitly called out above as still needing that verification).
